---
title: "College Outcomes Analysis"
author: "Richard Decal"
date: "October 26, 2017"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

Step 0: Read libraries  
```{r}
library(tidyverse)
library(purrr)
library(data.table)
```

Step 1: Set working directory  
```{r}
my_path <- "/Users/fanny/Google Drive/munge/college-outcomes"
setwd(my_path)
```


Step 2: Generate a list of files to be read in  
```{r}
files <- list.files("my_path", pattern="*.csv", full.names=TRUE) 
files
```





```{r}


read_my_files <- function(filename){
  x <- fread(filename, skip=1)
  return(x)

}

combined <- map_df(file_names,read_my_files)

```

#file_names
